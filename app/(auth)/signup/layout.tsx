export const metadata = {
    title: "Register"
}

export default function RegisterLayout({
    children,
}: {
    children: React.ReactNode;
}) {
    return (
<>
        {children}
        <h1> Footer </h1>
</>
    )
}